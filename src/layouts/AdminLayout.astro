---
import "../styles/global.css";
import {
  LogOut,
  LayoutDashboard,
  Home,
  Scissors,
  Package,
  Image,
  Users,
  MessageSquare,
  Phone,
  Link,
} from "lucide-react";

interface Props {
  title: string;
}

const { title } = Astro.props;
const { url } = Astro;
const { user } = Astro.locals;

const userEmail = user?.email || "Admin";

const navItems = [
  { href: "/admin", label: "Dashboard", icon: LayoutDashboard },
  { href: "/admin/hero", label: "Hero (Inicio)", icon: Home },
  { href: "/admin/services", label: "Servicios", icon: Scissors },
  { href: "/admin/products", label: "Productos", icon: Package },
  { href: "/admin/gallery", label: "Galería", icon: Image },
  { href: "/admin/team", label: "Equipo", icon: Users },
  { href: "/admin/testimonials", label: "Testimonios", icon: MessageSquare },
  { href: "/admin/contact", label: "Contacto", icon: Phone },
  { href: "/admin/navigation", label: "Navegación", icon: Link },
];

const currentPath = url.pathname;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Barber Ray Admin</title>
    <script is:inline>
      const theme = (() => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          return localStorage.getItem('theme');
        }
        return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
      })();
    
      if (theme === 'dark') {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
    </script>
  </head>
  <body
    class="bg-stone-950 text-stone-300 font-sans antialiased min-h-screen flex"
  >
    <aside
      class="w-64 bg-stone-900 border-r border-stone-800 flex flex-col fixed h-full z-10"
    >
      <div class="p-6 border-b border-stone-800">
        <h2 class="text-xl font-bold text-white tracking-wider uppercase">
          Barber Ray<span class="text-amber-500">.</span>
        </h2>
        <p class="text-xs text-stone-500 mt-1">Panel de Control</p>
      </div>

      <nav class="flex-1 overflow-y-auto py-4">
        <ul class="space-y-1 px-3">
          {
            navItems.map((item) => {
              const Icon = item.icon;
              const isActive =
                currentPath === item.href ||
                (item.href !== "/admin" && currentPath.startsWith(item.href));

              return (
                <li>
                  <a
                    href={item.href}
                    class={`flex items-center gap-3 px-3 py-2.5 rounded-lg transition-colors ${
                      isActive
                        ? "bg-amber-500/10 text-amber-500 font-medium"
                        : "text-stone-400 hover:text-white hover:bg-stone-800/50"
                    }`}
                  >
                    <Icon className="w-5 h-5" />
                    {item.label}
                  </a>
                </li>
              );
            })
          }
        </ul>
      </nav>

      <div class="p-4 border-t border-stone-800">
        <div class="flex items-center gap-3 px-3 py-2 mb-2">
          <div
            class="w-8 h-8 rounded-full bg-stone-800 flex items-center justify-center text-sm font-bold text-stone-300"
          >
            {userEmail.charAt(0).toUpperCase()}
          </div>
          <div class="flex-1 min-w-0">
            <p class="text-sm font-medium text-white truncate">{userEmail}</p>
          </div>
        </div>

        <form action="/api/auth/signout" method="post">
          <button
            type="submit"
            class="w-full flex items-center gap-3 px-3 py-2.5 rounded-lg text-stone-400 hover:text-red-400 hover:bg-red-500/10 transition-colors"
          >
            <LogOut className="w-5 h-5" />
            <span>Cerrar Sesión</span>
          </button>
        </form>
      </div>
    </aside>

    <main class="flex-1 ml-64 flex flex-col min-h-screen">
      <header
        class="h-16 border-b border-stone-800 bg-stone-950/80 backdrop-blur-md sticky top-0 z-10 flex items-center justify-between px-8"
      >
        <h1 class="text-lg font-medium text-white">{title}</h1>
        <a
          href="/"
          target="_blank"
          class="text-sm text-stone-400 hover:text-amber-500 transition-colors flex items-center gap-2"
        >
          Ver sitio en vivo
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            ><path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
            ></path></svg
          >
        </a>
      </header>

      <div class="p-8 flex-1">
        <slot />
      </div>
    </main>
  </body>
</html>
